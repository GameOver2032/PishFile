<div dir="rtl">

# تغییرات

## نسخه ۰.۱.۰ — فاز ۱: هسته‌ی آفلاین

### افزوده شد
- اسکلت کامل پروژه‌ی اندروید: Kotlin 2.0.21، Jetpack Compose (BOM 2024.12)، Room 2.6.1 با KSP، Gradle 8.9
- تم فارسی راست‌چین با فونت **وزیرمتن** (۴ وزن) و حالت روشن/تاریک
- دیتابیس محلی با ۶ جدول: پروژه، واحد، مشتری، پیش‌فایل، قسط، پیگیری
- **فیلدهای تخصصی املاک:** بلوک، طبقه، متراژ ناخالص/مفید/بالکن، جهت، منظر، ارتفاع سقف، پارکینگ و انباری، امکانات، مجوز ساخت، سند زمین، پیشرفت فیزیکی، قیمت تمام‌شده و…
- شماره‌گذاری خودکار پیش‌فایل (`PF-1405-0001`)
- ساخت خودکار جدول اقساط با دوره‌های ماهانه/دو ماهه/فصلی/شش‌ماهه/سالانه
- ثبت پرداخت کامل و جزئی، محاسبه‌ی خودکار مانده، دریافتی و درصد پیشرفت
- شناسایی خودکار اقساط معوق
- به‌روزرسانی خودکار وضعیت واحد بر اساس وضعیت پیش‌فایل (آزاد/رزرو/پیش‌فروش/تحویل)
- ساخت گروهی واحدها (طبقات × واحد در طبقه با متراژ پله‌ای)
- داشبورد مالی: ارزش قراردادهای فعال، دریافتی ۳۰ روز، مطالبات، معوقات، سررسیدهای نزدیک، کارهای امروز
- مدیریت پیگیری‌ها (تماس، بازدید، جلسه، پیام، یادآوری) با اولویت
- بکاپ کامل JSON + بازیابی، خروجی CSV پیش‌فایل‌ها و اقساط (سازگار با Excel فارسی)
- زیرساخت آماده‌ی همگام‌سازی: فیلدهای `remoteId`/`syncState`/`serverUpdatedAt` و قرارداد `SyncEngine`
- داده‌ی نمونه‌ی اولیه (پروژه، ۸ واحد، یک مشتری، پیش‌فایل با ۵ قسط و یک پیگیری)
- مستندات: README، معماری، مدل داده، راهنمای استفاده، نقشه راه، راهنمای مشارکت
- گردش‌کار CI گیت‌هاب برای ساخت APK debug در هر push

### نکات فنی
- حذف نرم (soft delete) در تمام جداول برای آمادگی همگام‌سازی آینده
- تاریخ شمسی با الگوریتم داخلی (بدون کتابخانه‌ی خارجی)
- مبالغ به تومان و با جداکننده‌ی هزارگان فارسی

### محدودیت‌های شناخته‌شده
- فایل APK خروجی هنوز با کلید انتشار امضا نشده (فقط debug)
- پیوست تصویر سند/چک و خروجی PDF در فازهای بعدی اضافه می‌شود
- بدون سرور؛ داده‌ها فقط روی همان دستگاه است

</div>
